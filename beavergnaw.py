import sys 


# read the first line
info = sys.stdin.readline().replace('\n','')
# iterate through the cases until the stopper is found
while(info != '0 0'):
    # create variables for diameter and volume
    d = int(info[0])
    miss_v = int(info[1])
    # create variable for total volume
    total_v = int(
    # get difference from total volume - volume missing
    
    
    
    
    
    
    # read next line